<?xml version="1.0"?>
<!DOCTYPE fontconfig SYSTEM "fonts.dtd">
<fontconfig>

  <!-- settings go here -->
    <match target="pattern">
        <test qual="any" name="family"><string>Nimbus</string></test>
        <edit name="family" mode="assign" binding="same"><string>DejaVu Sans Mono Nerd Font Complete</string></edit>
    </match>
</fontconfig>
